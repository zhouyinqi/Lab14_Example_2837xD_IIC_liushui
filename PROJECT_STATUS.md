# 当前进度

更新时间：2026-09-11

## 已完成

- DSP 工程已迁移并改名为 `HPD_BoardTest_DSP`，Qt 上位机位于同级 `UpperComputer` 目录。
- 已建立板型、硬件版本、能力位、Test ID 可用状态和 Qt 动态过滤机制。
- 已拆分 DSP_DI、DSP_DO、DSP_HDO 和 FPGA_DIDO 能力。
- 已实现 W5300/W5500、CAN、SCI、SPI、ADC、DI/DO、PWM-DIDO 等公共或低空板测试框架。
- 低空经济板已确认通过的实板项目包括 Ethernet、ADC 基础/X3/X4 注入、SPIB、DI 和 DO。
- 低压工业变频器 V04 的能力表及管脚矩阵已经录入。
- 低压 V04 已分配专用 Test ID `0x0400` 至 `0x040A`，Qt 结果表和板型过滤已接入。
- DSP 主机回归测试和 Qt 上位机回归测试当前通过。
- 已建立 `AGENTS.md` 开发约束、`PROJECT_CONTEXT.md` 长期项目上下文和本文件的同步进度记录机制。

## 正在处理

- 低压工业变频器 V04 专用外设测试开发。
- 当前开发起点为 `0x0400 LV_DI6_EXTERNAL` 六路 DI 外部注入测试。

## 下一步

1. 实现 `0x0400` 六路 DI 的 DSP 采样、单路识别、超时和诊断快照。
2. 增加 Qt 单路/多路引导、实时状态显示和自动结果刷新。
3. 完成主机回归测试后进行 V04 实板接线验证。
4. 按顺序继续 `0x0401` DO/软启动/风扇、`0x0402` HDO 和 `0x0403` STO。

## 未完成

- `0x0400`：低压板六路 DI。
- `0x0401`：三路 DO、软启动和风扇输出。
- `0x0402`：HDO。
- `0x0403`：两路 STO。
- `0x0404`：十三路 ADC。
- `0x0405`：PT100。
- `0x0406`：NTC。
- `0x0407`：六路 PWM。
- `0x0408`：保护、外部中断和故障清除。
- `0x0409`：模拟量输出。
- `0x040A`：旋变接口。

## 已知问题

- 低空经济板 PT4 120 欧姆测试读数异常，已与硬件确认属于硬件问题，软件不补偿。
- 低空经济板 PWM 测试中 DSP GPIO 读回正常，但外部端子无电压，需继续检查 DSP 后级缓冲、供电和端子链路。
- 低空经济板保护测试可识别 ADC 阈值变化，但故障位、D_BRK 和外部中断未按预期触发，硬件保护链待确认。
- 2026-09-11 已成功推送既有提交 c06fc49、f537253 到 origin/main，之前的网络阻塞已解除。
- `platforms/` 和 `tools/` 为与当前板卡程序无关的未跟踪目录，暂不纳入提交。

