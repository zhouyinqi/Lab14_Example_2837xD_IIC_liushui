# HPD控制模组测试上位机设计方案

## 1. 概述

### 1.1 设计目标

本上位机采用 Qt 开发，作为 DSP1 测试下位机的控制、显示和记录端。软件通过 Ethernet 与 DSP 通信，实现板载自检、外设连接后自检、HPD 注入测试命令下发和单项测试。

设计原则如下：

- 一键测试优先，保留单项测试用于定位问题。
- 测试结果以 DSP 返回的记录为准，上位机负责展示、日志和人工确认。
- CAN、SCIB 和 Ethernet 可由上位机自动产生测试报文，减少对第三方调试工具的依赖。
- 仅在允许的测试阶段驱动外部输出，DO 类测试保留人工确认和安全提示。

### 1.2 适用范围

适用于采用 F28377D DSP1、W5300 或 W5500 Ethernet、CAN、SCIB/RS485 及 FPGA 扩展 I/O 的测试板卡。DSP2、FPGA 内部逻辑和试验台物理量注入的具体实现不属于本上位机当前版本的控制范围。

## 2. 总体设计

### 2.1 系统组成

系统由 Qt 上位机、DSP 测试下位机、被测板卡和外部测试设备组成。

- Qt 上位机：发送测试命令，自动驱动 CAN、SCIB、TCP 测试，显示结果和日志。
- DSP 测试下位机：执行测试项，维护测试记录和通信待命状态。
- W5300/W5500：提供 DSP 与上位机之间的 TCP 通信接口。
- USB-CAN：作为 CAN 外部通信测试端。
- USB-RS485：作为 SCIB/RS485 外部通信测试端。
- PLC：用于 DI 状态翻转、EDO 目视确认及后续 AIAO/ADC 试验台测试。

图 2-1 总体架构图：Qt 上位机通过 Ethernet 向 DSP 命令邮箱发送命令；DSP 调用各测试模块并更新记录区；Qt 读取记录区。CAN、SCIB、Ethernet 和 PLC 分别连接至对应外设接口。

板型与 Ethernet 硬件为两个独立维度。DSP 上电后自动探测 W5300/W5500 并选择对应 GPIO 配置，上位机再由操作员人工确认实际板型。网络芯片类型不限制 SiC多传系统主控板、低压工业变频器或低空经济统型板的选择。

### 2.2 上下位机通信关系

上位机通过 TCP 协议向 DSP 发送控制帧，DSP 返回命令执行状态、测试管理器状态和指定 Test ID 的测试记录。

主要命令包括：

| 命令 | 用途 |
| --- | --- |
| `START_BOARD` | 启动板载/内部一键自检 |
| `START_EXTERNAL` | 启动外设连接后一键自检 |
| `START_HPD` | 启动 HPD 注入测试流程 |
| `START_SINGLE` | 在指定阶段执行单项测试 |
| `GET_STATUS` | 获取 DSP 当前模式、活动测试项和通信待命状态 |
| `GET_RECORD` | 获取指定 Test ID 的测试结果 |
| `STOP` | 中止当前测试并解除忙状态 |

### 2.3 测试阶段与执行流程

测试分为三个阶段：

1. `BOARD_ONLY`：板载/内部自检，不要求连接外部测试设备。
2. `EXTERNAL_CONNECTED`：外设连接后自检，要求按测试项连接网线、USB-CAN、USB-RS485、PLC 或存储器件。
3. `HPD_INJECTION`：HPD 注入测试，预留模拟输入和真实 ADC 采样两种模式。

图 2-2 测试流程图：上位机选择测试阶段并下发命令；DSP 执行测试并更新记录；上位机轮询状态、读取记录；若测试项需要通信报文或人工确认，则由上位机自动收发或弹出确认窗口；最终显示 PASS、FAIL、SKIP 或 MANUAL_CONFIRM。

## 3. 软件设计

### 3.1 Qt 工程结构

| 文件/模块 | 职责 |
| --- | --- |
| `MainWindow` | 主界面、测试流程编排、结果表、日志和人工确认 |
| `DspTcpClient` | DSP 控制协议 TCP 通信、请求队列和自动重连 |
| `DspTestProtocol` | 请求帧、响应帧、命令、测试阶段和结果枚举定义 |
| `EthernetTcpTestClient` | Ethernet 原始 TCP 链路、回显和稳定性测试 |
| `ScibSerialClient` | Qt SerialPort 自动执行 `0xA5 -> 0x5A` 测试 |
| `ZlgCanClient` | 通过 ControlCAN.dll 或 32 位桥接程序执行 CAN 自动收发 |
| `Tools/CanBridge32` | 兼容旧版 32 位 ControlCAN.dll 的桥接程序 |

### 3.2 主界面与功能分区

主界面包括以下区域：

- DSP Ethernet 连接：IP、端口、SCIB 串口、ZLG CAN 库、CAN 设备和通道配置。
- 一键测试：板载自检、外设连接后自检、HPD 注入自检和停止。
- 单项测试：先选择测试阶段，再选择允许的 Test ID；DO/HDO 测试需显式允许输出。
- 结果区：板载、外设、HPD 三类记录分别显示 Test ID、结果、错误码、原始值和测量值。
- 日志区：统一记录连接状态、测试过程、外设通信和人工确认信息。

### 3.3 DSP TCP 通信协议

`DspTcpClient` 使用单请求在途和发送队列机制，确保命令按顺序执行。每个请求设置响应超时；连接异常或响应超时时，客户端断开并按固定周期自动重连。

连接恢复后，上位机先读取 DSP 状态，再根据活动测试项、当前阶段和待刷新记录继续流程，避免因 Ethernet Socket0 重建造成结果丢失。

### 3.4 测试命令与结果记录

DSP 为每个 Test ID 维护记录，至少包含：

- `result`：NOT_RUN、RUNNING、PASS、FAIL、SKIP、WARN 等状态。
- `errorCode`：测试失败或超时原因。
- `rawValue`：底层状态字、寄存器值或报文统计值。
- `measuredValue`、`expectedMin`、`expectedMax`：测量值和判定范围。

上位机启动测试后，等待 DSP 返回待命状态，再按当前阶段逐项读取记录。单项测试只刷新被选 Test ID；一键测试刷新该阶段全部记录。

### 3.5 日志、状态刷新与自动重连

软件使用统一日志区记录通信和测试事件。DSP 状态响应中包含 CAN、SCIB、Ethernet 的待命、接收、应答和失败掩码，上位机据此显示“等待中”“已接收”“已应答”或“失败”。

Ethernet 测试协议与原始 TCP 测试共用 Socket0。执行 TCP_LINK、TCP_ECHO 或 TCP_STABILITY 时，上位机会暂时释放 DSP 控制协议连接；原始 TCP 测试结束后自动重新连接 DSP 并读取测试记录。

## 4. 外设测试设计

### 4.1 板载自检

板载自检执行不依赖外部测试设备的低风险测试项，包括系统启动、时钟、定时器、RAM、Flash、GPIO、ADC 基础框架、PWM 基础配置、EMIF 基础配置及 SPI 内部回环等。

该阶段用于确认 DSP 软件运行、基础寄存器配置和内部外设状态。板载自检通过不等同于所有板级接口和外设均已通过。

### 4.2 外设连接后自检

外设自检要求测试设备和接线已准备完成。DSP 执行配置检查和可自动执行的测试项；上位机根据待命状态自动启动 CAN、SCIB 和 Ethernet 报文测试；EDO 等需要人员观察的项目进入人工确认。

图 4-1 外设自检流程图：外设连接确认 -> DSP 启动外设阶段 -> CAN/SCIB/Ethernet 进入待命 -> Qt 自动通信测试 -> 刷新记录 -> EDO 人工确认 -> 输出结果。

### 4.3 CAN 自动测试

上位机通过 `ZlgCanClient` 调用 ZLG `ControlCAN.dll`，使用 USB-CAN 向 DSP 发送规定测试帧并等待应答。当前界面保存 DLL 路径、设备类型、设备索引和通道配置；旧版 32 位 DLL 通过 `HpdCanBridge32.exe` 调用。

判定依据为 USB-CAN 成功打开、测试帧发送成功、DSP 返回有效应答帧且无通信超时。CAN_TEST 等第三方工具在 Qt 自动测试期间必须关闭，避免独占 USB-CAN 设备。

### 4.4 SCIB/RS485 自动测试

上位机通过 Qt SerialPort 打开选定 COM 口，采用 `9600 8N1` 配置发送 `0xA5`，并等待 DSP 返回 `0x5A`。

判定依据为串口可打开、接收字节正确且未超时。SCIB 自动测试只在 DSP 已进入对应外设测试或通信待命状态后执行。

### 4.5 Ethernet 完整测试

单项测试中仅保留 `0x0305 ETHERNET（完整自动测试）` 入口。上位机依次执行以下子测试，结果区按 `ETHERNET / ...` 显示各 Test ID：

| 子测试 | Test ID | 判定内容 |
| --- | --- | --- |
| 芯片基础访问 | `0x0305` | DSP 按探测结果检查 W5300 或 W5500 基础寄存器和网络配置 |
| Socket 初始化 | `0x030D` | Socket0 可关闭、初始化并进入 TCP LISTEN 状态 |
| TCP_LINK | `0x030E` | Qt 与 DSP 建立一次 TCP 连接，DSP 正确识别并关闭连接 |
| TCP_ECHO | `0x030F` | Qt 发送 `BTST`，DSP 返回 `BTOK` |
| TCP_STABILITY | `0x0310` | 连续五次完成 `BTST -> BTOK` 收发 |

外设一键自检完成基础项目后，也会自动执行 TCP_LINK、TCP_ECHO 和 TCP_STABILITY，不依赖网络调试工具。

### 4.6 EDO 人工确认

EDO 外部输出测试为低风险驱动测试。DSP 按既定顺序驱动 EDO 通道，上位机提示人员观察 PLC 对应 DI 指示灯。人员确认三路指示灯按预期依次点亮和熄灭后，上位机将显示结果由 `MANUAL_CONFIRM` 标记为 PASS；未确认则标记为 FAIL。

HDO 由于接线和驱动方式不同，当前保留单项测试，不纳入外设一键测试。

## 5. 配置与异常处理

### 5.1 CAN 库与设备配置保存

上位机使用 `QSettings` 保存以下配置：

- `ControlCAN.dll` 完整路径及所在目录。
- ZLG 设备类型和设备索引。

下次启动时自动恢复上次选择；若 DLL 已移动或失效，选择窗口自动定位到上次使用的目录。首次升级时会尝试迁移旧设置。

### 5.2 通信异常与恢复

- DSP TCP 协议超时：上位机记录错误并自动重连。
- CAN 无法打开：提示检查驱动、DLL 位数、设备类型、设备索引及 CAN_TEST 占用状态。
- SCIB 超时：提示检查 COM 口、USB-RS485、9600 8N1 和 DSP 待命状态。
- TCP 测试超时：记录测试失败，重新连接 DSP 并读取对应记录。

### 5.3 测试中止与安全控制

用户可使用“停止”命令中止当前测试。停止时上位机清除待刷新流程；DSP 侧停止活动测试、关闭相关通信测试并解除输出授权。DO/HDO 单项测试需要用户在界面显式确认允许输出。

## 6. 当前实现与后续规划

### 6.1 当前已实现

- Qt 与 DSP 的 TCP 控制协议、状态读取、记录读取和自动重连。
- 板载自检、外设连接后自检、HPD 注入测试命令入口和单项测试入口。
- ZLG USB-CAN 自动测试，包含旧版 32 位 DLL 桥接支持。
- SCIB/RS485 自动 `0xA5 -> 0x5A` 收发测试。
- Ethernet 完整单项测试及外设自检中的自动 TCP 收发。
- EDO 目视人工确认、测试结果分区和统一日志。
- CAN 库路径、设备类型和设备索引的持久化保存。

### 6.2 后续规划

- 将 HPD 注入物理量、模拟输入和真实 ADC 采样参数配置纳入上位机界面。
- 根据 FPGA EMIF2 寄存器地址表，完善 DI、DO、AIAO 和 ePWM 的自动化测试与结果解析。
- 根据具体 I2C 器件、SPI SD 卡和后续板卡硬件配置，补充外设适配模块。
- 在稳定测试验证完成后，整理发布目录、运行依赖和版本发布说明。
