# 项目长期说明

## 项目目标

本项目用于 F28377D 控制板的板级自动化测试，由 DSP 下位机和 Qt 上位机组成。
当前维护的板型包括：

- SiC 多传系统主控板。
- 低压工业变频器 V04。
- 低空经济统型板 V01。

## 项目目录

- `HPD_BoardTest_DSP/`：CCS DSP 工程、测试执行逻辑和板型管脚矩阵。
- `UpperComputer/`：Qt 6 上位机、TCP/串口/CAN 控制和结果显示。
- Git 操作必须在 `C:\Users\zyq\Desktop\WORK\PCB_BOARD_TEST\HPD_BoardTest` 执行。
- 不再使用旧目录 `workspace_test/Example_CPU1/Lab14_Example_2837xD_IIC_liushui` 作为开发工程。

## 技术要求

- 每个 Test ID 必须绑定板型能力和硬件版本，只显示当前板卡实际具备的测试项。
- 区分“板卡具备、待开发、待验证、可测试”，不得仅凭能力位判断测试已完成。
- 保持 DSP_DI、DSP_DO、DSP_HDO、FPGA_DIDO 独立，不得混用。
- 不恢复 W5300 全部放行逻辑；W5300 和 W5500 必须采用各自的引脚与驱动配置。
- 保持现有 Test ID 和主机协议兼容；新增功能优先增加专用 Test ID 和回归测试。
- 低空经济板 PWM 按 DIDO 接口验证，不作为电机控制波形测试。未经用户明确授权，不启用功率驱动。
- 低空经济板 PT4 `0x031C` 已确认是硬件问题，不通过修改 DSP 或 Qt 阈值进行补偿，除非用户另行要求。
- 不删除现有文件、不回退用户修改、不提交 `build/`、`Debug/`、`.out`、`.map` 等生成文件。

## 当前进度

- DSP 与 Qt 已拆分为同级工程，目录迁移完成。
- 板型能力过滤、硬件版本矩阵和 Test ID 状态同步已经建立。
- 低空经济板的网络、CAN、ADC、SPIB、DI/DO等测试已有实现，部分外部接口已实板验证。
- 低压工业变频器 V04 已建立完整能力、管脚矩阵和 `0x0400` 至 `0x040A` 专用 Test ID。
- 低压 V04 专用 Test ID 当前仍为待开发，下一步从 `0x0400` 六路 DI 外部测试开始。

## 用户偏好

- 回复使用中文，结论先行，日志和界面文字保持简洁。
- 修改完成后运行相关 DSP 主机测试和 Qt 测试。
- 硬件已确认的问题只记录状态，不擅自用软件规避。
- 每次任务开始前读取 `AGENTS.md` 和 `PROJECT_CONTEXT.md`；每阶段结束更新 `PROJECT_CONTEXT.md` 的长期目标、硬件配置、技术决策、进度和待办，并同步 `PROJECT_STATUS.md`。过期信息替换，不累积。
- 代码修改完成后使用 Git 保存并推送 GitHub；网络失败时保留本地提交并明确报告未推送状态。


